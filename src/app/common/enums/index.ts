export enum WEIGHT_METRICS {
  LBS = "LBS",
  KG = "KG",
}

export enum ACTION_ITEMS {
  SET = "SET",
  EXERCISE = "EXERCISE",
}

export enum EXERCISE_TYPES {
  STENGTH = "STENGTH",
  CARDIO = "CARDIO",
}

export enum API_STATUS {
  IDLE = "idle",
  LOADING = "loading",
  ERROR = "failed",
  SUCCESS = "succeeded",
}
